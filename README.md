Task 2 - Data Insertion and Handling Nulls

In this task, I worked on basic SQL operations using SQLiteStudio.  
I created a new database, made a table, inserted records (including NULL values), updated a row, deleted another row, and finally displayed all the remaining data.

Below is everything I did step-by-step.

1. Creating the Database

First, I opened SQLiteStudio and created a new database:

- I clicked on Database → Add a database
- I created a new file named task2.db
- The database was added to the left panel

2. Creating the Table

After creating the database, I created a table called students.

The table has the following columns:

- id – INTEGER and PRIMARY KEY  
- name – TEXT and NOT NULL  
- age – INTEGER (can be NULL)  
- grade – TEXT (can be NULL)

